fastapi==0.115.0
uvicorn[standard]==0.32.0
python-dotenv==1.0.1
python-multipart==0.0.17
pdfplumber==0.11.4
pyyaml==6.0.2

# Database dependencies
psycopg2-binary==2.9.11
sqlalchemy==2.0.44
alembic==1.17.2

# R2 Storage
boto3==1.35.0

# Rate limiting
slowapi==0.1.9

# Authentication
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
bcrypt==4.2.0
PyJWT==2.8.0

# Excel support
openpyxl==3.1.5
xlrd==2.0.1

# Email
resend==2.0.0
email-validator==2.1.0

# EmbodyBench setup-validation subagent (Claude Haiku); optional —
# bench_validation.py degrades gracefully if this is missing or
# ANTHROPIC_API_KEY is unset.
anthropic==0.40.0

# Passport → DOCS command generator (passport_router). Production reads the MRZ
# via Aliyun 国际护照识别 (deterministic parsing); OpenAI gpt-4.1-mini is a gated
# benchmark-only fallback. Both degrade gracefully if their keys are unset.
openai==1.109.1
alibabacloud_ocr_api20210707==3.1.3
alibabacloud_tea_openapi==0.4.5
alibabacloud_tea_util==0.3.14
Pillow==12.2.0
